# Shell Agent Notes

Decision records for the Electron shell live here, following the same layout as the Web kernel's notes ([implemented/](implemented/)). Kernel decisions stay in the submodule's own `.agents/notes/`; this tree covers shell-owned choices only.

Non-trivial shell changes include a note in the same commit (see the repository AGENTS.md).
