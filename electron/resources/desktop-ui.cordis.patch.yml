# The Electron-managed local page mounts the desktop-only Settings contributions.
# Inert in ordinary browsers; the managed Electron preload activates them.
- insert:
    - id: ui-desktop-electron
      name: '@deepseek-ai/dsh-client-ui-desktop-electron'
