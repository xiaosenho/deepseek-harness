/**
 * Electron adaptive directory-picker surface, node half. The browser half
 * selects one directory flow from the page connection authority.
 */

/** Host plugin body: no host-side behavior for this client surface. */
export function apply(): void {}
