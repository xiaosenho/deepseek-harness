/** Host loader entry for Electron-only browser controls exported from `./client`. */

/** Host plugin body; desktop behavior is owned by Electron main and the browser plugin. */
export function apply(): void {}
