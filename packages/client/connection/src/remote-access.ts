/** Browser-safe names shared by remote-access URL producers and consumers. */

/** Fragment parameter carrying a remote-access token into the browser. */
export const REMOTE_ACCESS_FRAGMENT_PARAM = 'dsh-access'

/** Session cookie carrying remote-access proof on same-origin requests. */
export const REMOTE_ACCESS_COOKIE_NAME = 'dsh_remote_access'
