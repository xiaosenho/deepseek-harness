# Git hooks (lefthook). The shell keeps one fast local checkpoint; CI owns
# the full gate matrix. The Web kernel enforces its own hooks inside the
# deepseek-harness-web submodule.
# Install: `pnpm run postinstall` (lefthook install).

pre-commit:
  jobs:
    - name: whitespace (staged)
      run: git diff --cached --check
